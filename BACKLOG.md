# Backlog

Ideas and known follow-ups, not yet scheduled. Pull items off this list into active work when ready.

**Scope, as of the git-flow process pass (2026-07-19):** this file is now for items that need to land on `main` independently of any single feature — process/harness improvements and out-of-band bugs unrelated to whatever feature is in flight. A feature's own backlog — polish, deferred work, findings scoped to it — lives in its own `specs/<feature>/backlog.md` instead (`specs/README.md`). Existing entries below predate this split and haven't been sorted retroactively.

## Loop-Based Development Foundation

Setup to work through before resuming feature work under the Ralph loop pattern. Starting small: three subagents (coder, reviewer, diagnosis), run by hand. See `.claude/skills/ralph-loop/SKILL.md` for the overall state machine (spec/plan, the task loop, the QA loop, landing); `.claude/skills/ralph-git/SKILL.md` for the git mechanics it runs on.

### Spec docs
- ~~Spec template + location.~~ Done — see `specs/README.md`.
- ~~Exercise Calibration spec.~~ Done — see `specs/20260705-exercise-calibration/spec.md`.
- ~~Exercise Calibration `plan.md`.~~ Done — see `specs/20260705-exercise-calibration/plan.md`. 11 tasks, first blocked on the seed JSON being supplied (task 1).

### Also worth deciding before iterating
- **AGENTS.md as source of truth.** Write `AGENTS.md` as the real instructions doc; have `CLAUDE.md` `@`-reference it so this harness and any AGENTS.md-native tooling read the same file instead of two drifting copies.
- **A single "current task" pointer.** A small file (e.g. `NOW.md`) the loop always points agents at — distinct from this backlog, which is "someday": this is "what the coder is working on this iteration."
- **Iteration boundaries / stop conditions.** Since the loop is run by hand at first, decide up front what one manual iteration covers (one backlog item? one spec section?) and what "done for the session" looks like, so stopping is a decision, not a drift.
- **AsyncStorage test doubles.** All app state lives in AsyncStorage — agents need a standard way to seed/inspect/clear it in tests and dev builds rather than each task inventing its own. Not yet decided; carried over from harness setup since it didn't come up while wiring Jest/RNTL.
- **Skill-creation guidance.** When does something belong in a new repo-scoped skill vs. `CLAUDE.md`/an agent doc, and how should skills get maintained as they accumulate? Came up while deciding where to document the `theme.ts` placeholder pattern; no answer yet.
- **Where agent-discovered deferred-architecture concerns belong.** Coder-discovered gaps that don't block the current task (e.g. the Exercise Calibration `theme.ts` placeholder) currently get noted in this file, but this file reads as the user's own idea-dump, not a queue for the agent to flag things that might need triage or handling soon. Worth a separate mechanism later; fine to keep using this file for now while still learning the loop.
- **The "seam" / anti-corruption-layer pattern deserves a closer look.** Used ad hoc for the `theme.ts` placeholder (`specs/20260705-exercise-calibration`); worth reading up on the general pattern in case there are useful conventions to adopt beyond what got improvised here.
- **Landing docs-only commits on `main` independently, mid-feature.** `ralph-git`'s escape hatch already covers cherry-picking a docs commit out of scratch history so it isn't lost, but doing this well — landing it on `main` on its own schedule without it depending on the feature it happened to be made during — got complex enough in practice (see the `task/N-done` tag-repointing dance from the Exercise Calibration loop) that it's worth revisiting this part of the flow more critically rather than trusting the current escape hatch as final. One symptom worth addressing as part of that review: `main` has been accumulating local-only commits for multiple sessions now (20+ ahead of `origin/main` as of the Exercise Calibration loop) with no push cadence defined — decide then whether/when `main` should get pushed, rather than letting it drift indefinitely. **Partially addressed, 2026-07-19 git-flow process pass:** the feature-scoped-vs-repo-root backlog split (`specs/README.md`) and the per-task pull-`main` checkpoint (`ralph-loop` Stage 2) directly target the commit-density and staleness symptoms here. Still open: `main`'s push cadence to `origin` — nothing decided yet, still drifting.
- ~~No escape hatch for cross-cutting "chore" commits.~~ **Resolved, 2026-07-19 git-flow process pass** — see `ralph-git`'s "Chore commits" section: a `chore/<slug>` tag for feature-local hygiene, compacted at a per-task boundary-time check; out-of-band fixes skip it and go through the docs escape hatch straight to `main` instead. One piece deliberately left open: a `chore(scratch):` commit discovered mid-task has no automated extraction if the task around it is later thrown away whole — recovered by hand (backlog it, discard, reapply) until that's actually happened once and it's clear what tooling is actually needed.
- **Compaction has no scripted safety net.** Two mistakes were hand-caught, not prevented, compacting the Exercise Calibration loop's finding-2 bugfix (`GIT-FLOW-ISSUES.md` section 3): an unscoped `git commit -m` swept unrelated staged files into the wrong commit, and a `git cherry-pick` failure left a stuck sequencer state that a naive retry didn't clear. A small script mirroring `engineering-log/append.sh`'s pattern — wrapping `reset --soft`/tag/cherry-pick with a dry-run and automatic safety-branch creation — would catch both mechanically instead of relying on checking `git status` after every step. Tabled in the 2026-07-19 git-flow process pass; revisit after the next feature, once it's clearer whether the new per-task boundary-check habit (pulling `main`, compacting chores every task instead of in occasional large batches) already reduces how often risky, large-scale manual compaction happens at all.
- **No-op rebase can still conflict, because of an embedded merge commit.** `ralph-git`'s "Landing a feature" runs `git rebase main` unconditionally. If the feature branch already fully contains `main` (confirmed via `git merge-base --is-ancestor`) but also has an old embedded merge commit somewhere in its own history, a standard rebase flattens history and replays that merge's content as a fresh patch — producing a spurious conflict against content `main` already has by a different path (`GIT-FLOW-ISSUES.md` section 8 has the concrete instance, from Exercise Calibration's landing). Candidate fix: check `is-ancestor` first and skip the rebase entirely when true, rather than trusting `git rebase`'s exit behavior. Tabled in the 2026-07-19 git-flow process pass; revisit after the next feature lands.
- **Verify custom `subagent_type`s resolve correctly once the driver runs from inside the worktree.** `.claude/agents/*.md` files added on a feature branch weren't invokable by name from the driver's own session during the Exercise Calibration loop (`subagent_type: "diagnosis"` wasn't in the registry until the feature landed) — worked around by launching as `general-purpose` with the agent doc's path embedded in the prompt (`GIT-FLOW-ISSUES.md` section 5). The 2026-07-19 git-flow process pass moved the driver to run from inside the feature's worktree instead of the main checkout (`ralph-git`'s "Starting a feature's worktree"), which may resolve this as a side effect if the registry follows the driver's cwd rather than a fixed main-checkout location. Check the first time a new agent type gets added mid-feature on the next feature — no fix needed yet, just confirm.
- **No provisioned iOS simulator for agent-driven UI validation.** Discovered mid-Exercise-Calibration-loop (after task 7 landed) that `xcrun simctl` has no installed runtime — every listed device profile is `(unavailable, runtime profile not found)` — so an agent can't currently launch/screenshot the app itself; the user validates manually via Expo Go instead. Set up a simulator runtime an agent can drive (screenshot, tap, type) for real validation instead of trusting typecheck/lint/tests alone. Likely pairs with two related, not-yet-designed pieces: (1) additional validation gates beyond the current tsc/lint/test trio — something that actually exercises rendered UI; (2) formalizing simulator-driven checks as a real step in the coder/reviewer flow (`.claude/agents/coder.md`, `.claude/agents/reviewer.md`, and/or the `testing` skill), not just an ad hoc "go look at it" step.
- **No strategy for "out-of-band" bugs discovered outside any task's scope.** Surfaced when the user tried validating tasks 1-7 in Expo Go for the first time and hit a real crash (co-located `*.test.tsx` files under `src/app/` were being swept into Expo Router's route bundle and crashing on-device — see the `metro.config.js` fix folded into `task/7-done`). Nothing in the coder/reviewer loop currently answers "who investigates this, who decides the fix, and which tier/task does it land under" for a bug that isn't part of any task's acceptance criteria — this time it got diagnosed and fixed by the orchestrating session directly (not a coder subagent) and folded backward into the current task's boundary tag, by explicit user decision made in the moment rather than an established convention. Related to, but distinct from, the "no escape hatch for cross-cutting chore commits" item above — that one is about hygiene/refactor sweeps with a clear author (a coder mid-loop); this is about *undiscovered breakage* found by manual validation, possibly gated behind having an actual UI-validation step at all (see the simulator item above — this bug would likely have been caught immediately if any simulator-driven check existed). Also note: **`main`'s own hello-world app has the identical latent bug** (`src/app/index.test.tsx` under `src/app/`) — the fix hasn't been applied there yet, only on the feature branch. **Confirmed live 2026-07-19:** this actually blocked the user mid-QA-loop — they hit the exact bundling crash trying to launch `main` in Expo Go to manually validate the finding-2 bugfix. Worked around by testing from the feature worktree instead, where the `metro.config.js` fix already exists. Explicit user call: not fixing `main` independently right now — the "permanent fix" (whatever form it takes: cherry-picking the config fix onto `main` standalone, or resolving this via the broader "landing docs-only commits on `main` independently, mid-feature" item below) is intentionally deferred to the post-landing git-flow overhaul, alongside the `EnterWorktree` pinning fix. **Partially addressed, 2026-07-19 git-flow process pass:** this class of fix now has a clear home — `ralph-git`'s "Chore commits" section routes anything that would "still make sense if the feature were deleted" (exactly this case) through the docs-style escape hatch straight to `main`, independent of the feature branch, instead of waiting for the feature to land. `main`'s own copy of this specific bug is still unfixed, but the process gap that left it unfixed is closed going forward.
- **`npm test` from `main` picks up duplicate copies of every test file from inside `.claude/worktrees/<feature>/`, and they fail.** Discovered 2026-07-19, immediately after landing Exercise Calibration: with the feature's worktree still present on disk, running `npm test` from the repo root discovered and ran the *same* test files twice — once at their real path, once again via the worktree directory sitting inside the repo tree — because nothing in the Jest config excludes `.claude/worktrees/**` from test discovery. The worktree copies fail outright (`Invariant Violation: __fbBatchedBridgeConfig is not set`, `AsyncStorage is null`), not because of any real bug, but because they end up running against a mix of root's and the worktree's separate `node_modules` trees, breaking React Native's native-module mocking. The real, non-duplicated tests all pass — this is purely a discovery-scope gap, the direct Jest-side analog of the `metro.config.js` fix above (test files getting swept into a scan they were never meant to be part of). Under this repo's worktree-per-feature convention, `.claude/worktrees/` will contain a real worktree any time a feature is actively in progress — meaning **`main`'s own `npm test` is not reliably green whenever a worktree coexists**, which will recur on every future feature unless fixed (a `testPathIgnorePatterns` entry for `.claude/worktrees` is the obvious candidate). Not fixed now — explicit user call to defer to the post-landing git-flow session, same bucket as the `EnterWorktree` and `main`-missing-fixes items above, since it's the same class of "worktree-as-a-directory-inside-the-repo" infrastructure gap.
- **No formalized process for updating a spec/plan mid-feature.** `specs/README.md`'s "Changing a spec mid-feature" section says *that* it must happen deliberately as its own agent-docs commit, but not *how* — the actual mechanics have been improvised twice now in the Exercise Calibration loop: task 4's review surfaced a rendering concern and got resolved by inserting tasks 5-6 and renumbering 7-13 to what followed; then a manual-validation gap (missing home-screen entry point, discovered after task 7) got resolved by inserting a new task 8 and renumbering the old 8-13 to 9-14. Both times: no fixed rule for *where* a new task gets inserted vs. appended, who decides that placement, or how to avoid stale task-number references elsewhere (this is exactly why the coder's comment-hygiene convention bans plan.md task numbers in code comments — discovered because renumbering already broke a reference once). Worth a real convention: e.g. always append and let ordering be informal, or a lighter task-ID scheme that survives renumbering (letters/decimals instead of sequential integers), or explicit criteria for "insert now" vs. "append."
- **An architecture-review agent plugging into feature landing, doing big-picture analysis over the whole `engineering-log.md`.** Companion to the `engineering-log` skill (`.claude/skills/engineering-log/`, wired into `coder.md`/`reviewer.md` as of the Exercise Calibration loop) — that skill deliberately only captures granular, per-task implementation decisions as they're written, not big-picture synthesis (the name changed from "architecture-log" to "engineering-log" specifically to mark that distinction). Nothing yet reads the accumulated log as a whole and synthesizes it: spotting a repeated pattern across entries, flagging where deferred debt has piled up, noticing when several "judgment call" entries actually add up to a design question worth revisiting deliberately rather than three separate footnotes — that bigger-picture, "architecture" framing belongs here instead. Natural fit at feature-landing time, alongside the whole-spec conformance pass below. Not designed yet — what it reads beyond the log itself (probably the diffs/review files too), what it outputs (a synthesis appended to the log? a separate landing-summary doc?), and whether it's a single pass or something closer to the reviewer's per-task discipline applied at feature scope. **Reinforced by the QA loop's finding-2 diagnosis** (`diagnoses/stale-hide-calibrated-filter.md`): the diagnosis surfaced a coding-pattern concern (annotating a read of the static exercise JSON with per-user calibration state at the point of use, rather than a clean separation between the immutable shared exercise catalog and per-user data) that isn't a defect a better spec or diagnosis template would have prevented — it's a pattern judgment call. That kind of thing is exactly what per-task review and bug diagnosis structurally can't catch (narrow, fresh-context, scoped to one symptom or one task's acceptance criteria) and exactly what this flow is for: a point where the user reviews accumulated implementation patterns across a whole feature and gives direct guidance, deliberately *not* injected mid-loop into an in-flight task or bugfix. Underscores this is a load-bearing gap, not a nice-to-have — worth prioritizing design of this once Exercise Calibration lands.
- **A whole-spec conformance pass before landing, not just per-task acceptance criteria.** Discovered while thinking through Exercise Calibration's landing step after task 14 landed: the reviewer checks each task's diff against `plan.md`'s acceptance criteria for that one task, but nothing ever re-reads `spec.md` end-to-end as a single unit before a feature lands. This is exactly the kind of gap that produced task 8's missing home-screen entry point — `spec.md`'s non-goals didn't mention it, no task ever wired `src/app/index.tsx` up, and a per-task check would never have caught it since it wasn't any task's job. Add this as an explicit landing-time step, distinct from per-task review: someone (the user, or a fresh-context reviewer) walks `spec.md` behavior-by-behavior against the landed app before rebase/squash/merge.
- **A human testing plan, generated before landing.** Same discussion as the whole-spec conformance pass above, but for manual QA rather than a read-through. Timing: after code-complete, before landing — generating it earlier risks it mirroring `plan.md`'s task list instead of independently checking `spec.md`'s actual behavior. Location: `specs/<feature>/testing.md`, alongside `spec.md`/`plan.md`, so it's a durable artifact of the feature rather than a throwaway note. Access: the coder shouldn't see it during implementation — it should stay a check against the spec, not become a second checklist the coder ends up coding to; a reviewer/tester step generating it, with the user running it by hand, is the current best guess. Update: the Exercise Calibration loop's actual pre-land human review (see `.claude/skills/qa-loop/`) landed on capturing raw findings in `specs/<feature>/qa-findings.md` instead — reconcile the two once a generated-testing-plan step actually gets built, rather than letting the filenames silently diverge.
- **A concrete flow for reproducing a reported bug, not just reasoning about it statically.** Came up designing the `bug-diagnosis` skill (`.claude/skills/bug-diagnosis/`) for the QA loop (`.claude/skills/qa-loop/`): diagnosis currently builds its causal chain from code reading, tests, and git history only, and explicitly says so when that's not enough to close the evidence gap. Actually driving the app to reproduce a symptom live is out of scope for now — it's blocked on the same gap as the "No provisioned iOS simulator for agent-driven UI validation" item above, plus needs its own design (what counts as a reproduction, who runs it, how it's recorded) rather than folding into diagnosis as an afterthought.
- **A flow for landing design changes before a feature ships, not just deferring everything to the backlog.** Surfaced designing the QA loop (`.claude/skills/qa-loop/`): its triage step routes UX/design-change findings straight to this backlog file, on the assumption they're always a later-session followup. That's not always right — a bad-enough interaction (e.g. Exercise Calibration's muscle-group/equipment filter pills, reported during its pre-land human review) might need fixing before the feature ships rather than shipping as-is and coming back to it. No process for that judgment call exists yet; `qa-loop` currently just flags it to the user in the moment instead of deciding it.
- **An intermediate draft doc before a diagnosis report is finalized.** Raised while designing `bug-diagnosis`'s Hypothesis-write-last discipline (`.claude/skills/bug-diagnosis/`): right now the `diagnosis` subagent investigates and writes the polished eight-section report in one pass. A cleaner separation might have it work in a scratch/intermediate doc first, then only produce `specs/<feature>/diagnoses/<slug>.md` once it's actually confident in the write-up — closer to how the coder's task-scratch commits stay separate from the compacted task commit. Not needed yet with a single bug in flight; revisit once the QA loop is routinely handling enough bugs at once that a rougher, not-yet-committed-to draft stage would pay for itself. **Update, first real end-to-end run of the QA loop (Exercise Calibration finding 2, `diagnoses/stale-hide-calibrated-filter.md`):** the diagnosis doc read well on its own, but `qa-loop` Step 4 currently hands it to the coder directly as "the spec for this fix" with no intermediate step — confirmed live that this is thin. A diagnosis doc explains *cause*, not *what to build*; converting it into something closer to a standard fix-spec/plan the coder implements against (the way `plan.md` tasks work for the main loop) is a real gap, not just a hypothetical one. This flow needs further development before leaning on it for a multi-bug pass.
- **Surfacing interdependencies between bugs diagnosed in the same QA-loop pass.** `qa-loop`'s fix step now diagnoses every bug found in a pass before fixing any of them, specifically because fixing one might change another's behavior — but that's only a partial mitigation: diagnosing everything up front doesn't actually detect or record when two diagnoses touch the same code path or would contradict each other's fix, it just delays fixing until all the diagnosis work is done. With only one bug in the Exercise Calibration QA pass, this hasn't mattered yet. Needs real design once a pass produces multiple bugs at once: does a later diagnosis check earlier ones for overlap, does the driver do a cross-diagnosis read before starting fixes, does fix ordering matter, etc.
- **Reviewer (and other read-only-by-design subagents) have no enforced boundary against destructive writes — relies entirely on the agent doc's prose, not on any actual permission scoping.** Concrete incident, 2026-07-19, finding-2 bugfix review (`bugfix/stale-hide-calibrated-filter-review`): the `reviewer` subagent, whose entire job is investigation and writing to exactly two designated output artifacts (`.claude/review/*.md`, an `engineering-log.md` append), ran `git checkout <rev> -- .` without checking `git status` first while comparing output across revisions — briefly clobbering three uncommitted files and reverting two `BACKLOG.md` entries. It self-corrected via `git fsck`/reflog and reported the incident transparently; the driver independently verified full recovery (byte-identical content, only a benign staged-vs-unstaged index difference left behind). No data was actually lost, but the only thing that stopped this from being real is the agent's own self-correction — nothing in the harness prevented the write in the first place. `reviewer.md` already states in prose that it may only touch its two output files and "never edits application code," but Bash access is unrestricted, so nothing stops it from running arbitrary destructive git commands; the same is presumably true for `diagnosis.md`'s prose-only "no commits, no tags" boundary.

  A first pass at researching what's actually configurable today (via `claude-code-guide`, not independently verified against current docs — treat as a starting point, not settled fact): custom agents can restrict access to *entire tools* via a `tools:` frontmatter field (confirmed real — visible in this harness's own agent-type listing); `.claude/settings.json` supports Bash allow/deny rules by command-glob pattern (e.g. deny `git checkout *`/`git reset *`/`git add *` while allowing `git status`/`git log`/`git diff`), but these are session/project-wide, not scoped to a single subagent invocation; `PreToolUse` hooks can inspect a command's actual content (not just the tool name) and block it. Two open questions this pass couldn't resolve with confidence: whether a hook or permission rule can be scoped to only fire for a specific custom agent type (vs. every Bash call in the session), and whether per-worktree `.claude/settings.json` is even respected differently from the root checkout's (this repo already gives worktrees their own `.claude/agents/`+`.claude/skills/`, which is why the question came up — but settings.json may not follow the same resolution rules as those).

  Not being designed or fixed now — explicit user call to revisit after the git-flow overhaul (see the `EnterWorktree` item below, which this is closely related to: both are "the loop's actual git safety needs harness-level enforcement, not just prose conventions" problems). When it is designed: the reviewer and diagnosis subagents are the obvious first candidates for whatever mechanism gets built, since both have a clean, narrow, already-documented set of allowed writes and no legitimate reason to ever run a git write command.

  **Priority flag, 2026-07-19 git-flow process pass:** explicit user call to revisit this specific item *before* the next feature starts, not after — takes priority over the other items tabled in that same pass (compaction safety, the no-op-rebase check).

- **`EnterWorktree` fails intermittently for subagents, forcing an absolute-path workaround.** First seen at task 6's coder, recurred at task 9's coder, both times with "the current working directory ... is the repository root, not an isolated worktree — switching is only available to sessions whose working directory is inside a worktree of this repository" on first entry, even though the tool's own documented behavior says this should work when the given path is in `git worktree list` (which it was, both times). Every other coder/reviewer invocation across the Exercise Calibration loop (roughly a dozen) hit no such failure. Worked around each time by having the affected subagent root every subsequent Read/Edit/Bash call at the absolute worktree path instead of relying on session pinning — functional, but it's a workaround baked into `coder.md`/`reviewer.md`'s "Before you start" instructions rather than a fix. No reproducible trigger identified yet (not tied to a specific task, agent type, or timing pattern noticed so far). Worth either filing as a real harness bug report with the two known repro instances, or digging further to find what's actually different about the failing invocations. **Tabled, 2026-07-19 git-flow process pass:** the driver now runs from inside the worktree instead of the main checkout (`ralph-git`'s "Starting a feature's worktree"), which may change or eliminate this as a side effect — retest naturally on the next feature before assuming it's still broken. Also worth a fresh empirical check regardless: the current `EnterWorktree` tool docs already describe supported behavior matching both known failure variants (first-entry switching when the path is in `git worktree list`, switching from an already-pinned subagent), which doesn't match the error text recorded here — possibly already fixed upstream since these were hit.
- **A dedicated "readability" agent/breakpoint, separate from the reviewer.** Surfaced during a manual code-review session (tasks 1-8, Exercise Calibration). The `reviewer` subagent checks spec-conformance and correctness; nothing in the loop evaluates code and comments purely on how easy they are to *understand* — variable/function naming, comment quality, whether a fresh reader (human or a future fresh-context agent) could follow the code without re-deriving intent. Conflating that with correctness review risks either skipping it under time pressure or diluting the reviewer's focus. Wants its own loop breakpoint, not folded into `reviewer.md`. Not yet designed: where it runs (every task? every feature-landing? on demand?), what it's allowed to touch (probably comments/naming only, not logic), and whether its output is another `.claude/review/*.md`-style file or something else.
- **Differentiating classes of features/changes in this backlog, and in the loop generally.** Surfaced testing the QA loop end-to-end on Exercise Calibration finding 2: this file currently treats every entry the same — a one-line UX tweak sits next to a load-bearing data-model question (see the architecture-review-agent item above) with no way to tell them apart at a glance, and the loop itself has no notion that different classes of work (a small bugfix, a UX polish item, a genuine architecture decision, a new feature) might warrant different processes rather than all funneling through the same task/bugfix cycle. Related to, but distinct from, the "Where agent-discovered deferred-architecture concerns belong" item above (that one is about *where* a concern gets recorded; this one is about *what kind of thing* it is once recorded, and whether that should change how it's handled). Not designed yet — could be as light as a tagging convention on backlog entries, or as involved as distinct triage paths per class. Worth prioritizing given how much this session reinforced the need.
- **`GIT-FLOW-ISSUES.md` (repo root, alongside this file) needs a real home and format — and more generally, where do reports like it belong at all?** Written 2026-07-19 as a detailed, evidence-based trace of every git/harness-mechanics issue hit running this feature's Ralph loop by hand (`EnterWorktree` failures, the recurring chore-commit-gap, two compaction mistakes, the reviewer's destructive-git-write incident, worktree-local agent definitions being invisible to the `Agent` tool's registry, `main`'s missing fixes, tag-repointing after rebase) — explicitly ad hoc for now, dumped at the repo root because there's nowhere better yet. User's stated plan: address this properly in a dedicated backlog-organizing session, before the next feature starts, alongside the "Differentiating classes of features/changes" item below (this doc is itself an instance of that gap — a big pile of undifferentiated findings with no format or filing convention). **Generalized, 2026-07-19 git-flow process pass:** most of this specific doc's content got acted on this session (see the resolved/tabled/partially-addressed items above and in `ralph-git`/`ralph-loop`), but the doc itself is still sitting at the repo root with nowhere better to go — and this isn't only about this one file. Any future ad hoc investigation/trace report (a debugging session's findings, a process retro, anything that's evidence-gathering prose rather than a spec/plan/backlog entry) will want a home too. Repo root isn't a real answer long-term. Needs an actual decision — a `reports/`-style directory, folding into an existing convention (a spec directory? the `engineering-log`?), or something else — not made yet.
- **Where a human validation checkpoint belongs in the bug-fix loop, depending on bug class.** Surfaced live during the finding-2 bugfix (`bugfix/stale-hide-calibrated-filter-review`): `qa-loop` Step 4 currently only has the *reviewer* check the diagnosis's Human validation description, after the coder is done — there's no point where the user, who originally found and can readily reproduce the bug by hand, tries the fix themselves before it goes to the reviewer at all. For a finding the user already knows how to trigger in the running app, testing the coder's output directly seems like it should happen *before* spending a review pass on code that might not even fix the reported symptom. Whether that's true for every bug, though, is exactly the open question — a bug that's hard for the user to reproduce, or an internal correctness issue with no visible repro, might not support this at all. This is a concrete instance of the "Differentiating classes of features/changes" item above: the loop needs some notion of bug class (readily human-reproducible vs. not, at minimum) to decide whether a "user tries it first" checkpoint belongs between coder and reviewer, and if so, how it's triggered and what happens if it fails (back to coder? back to diagnosis?). Not designed yet.

## Theming

- **Real theme system.** `src/constants/theme.ts` (added in Exercise Calibration task 4, for the `/calibrate` list screen) is a temporary placeholder, not a finished design system — see the file's header comment. It covers only the colors/typography that screen needs (primary/secondary muscle highlight, equipment text, calibrated badge, card/page background). Replace it with a proper theming system rather than growing it in place as more screens need colors.

## Exercise Calibration

Thumbs-based effort scale, muscle-group filter, and equipment filter
are now in scope in `specs/20260705-exercise-calibration/spec.md` rather than
backlogged — nothing left here for those. "Sort by muscle groups hit"
was considered and deliberately dropped as a non-goal in that spec.

- **More intuitive muscle group / equipment filter UI, not horizontally-scrolling pills.** Reported during the feature's pre-land human review (see `specs/20260705-exercise-calibration/qa-findings.md`, finding 1, triaged via `.claude/skills/qa-loop/`) — scrolling through and tapping pills to filter didn't feel intuitive. No alternative design chosen yet (visible-state chips, a sheet/modal picker, a multi-select list, something else) — needs a design decision before it's an actionable task, not just a fix.
- **Calibration form: keyboard covers the notes field with no way to dismiss it directly.** Found 2026-07-19 while manually validating the finding-2 bugfix. On the calibration form, the on-screen keyboard (opened to type notes) covers the notes input and there's no visible way to dismiss it (no "Done" affordance, tapping outside doesn't close it) — the only way found was the incidental side effect of tapping the mental-effort thumbs button, which isn't a real fix. Needs a proper dismiss affordance (e.g. a toolbar "Done" button, tap-outside-to-dismiss, or `KeyboardAvoidingView` tuning) — deferred to a future loop pass, not blocking finding 2's landing.
- **Human-readable muscle group names.** `data/exercises.json`'s
  `primaryMuscles`/`secondaryMuscles` values are raw identifiers
  (`upper_back`, `quads`, `lats`, etc.) rendered as-is by
  `ExerciseCard` and (once task 9 lands) the muscle-group filter —
  no display-name mapping exists. Needs a `muscleGroupId ->` display
  label lookup (e.g. `upper_back` -> "Upper Back", `quads` ->
  "Quadriceps", `lats` -> "Lats" or "Latissimus Dorsi") used
  everywhere a muscle group is shown or listed as a filter option,
  rather than each call site formatting the raw string itself.

## My Gym (new section)

- New section for the user to record what equipment they have
  available. No longer a blocker for the calibration equipment filter
  (that filter is transient/derived from the exercise database per
  `specs/20260705-exercise-calibration/spec.md`) — this is still worth building
  for its own sake whenever a feature actually needs a persisted
  equipment inventory.
